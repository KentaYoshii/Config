# Switch tmux prefix to C-Space
unbind C-b
set-option -g prefix C-Space
bind-key C-Space send-prefix

# Switch H and V splits
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Reload
bind r source-file ~/.tmux.conf

# Mouse
set -g mouse on
